// Re-export shared types from the monorepo types package.
// Import from here within the web app, or directly from '@fastify-admin/types'.
export type { Field, EntityMeta, EntityCount } from '@fastify-admin/types'
